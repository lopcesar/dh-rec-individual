/* import React from "react";
import Temporada from "./Temporada";

function Serie (props){

    const temporadas = Temporada;

    return (
        <div>
            <h2>Titulo de la Serie: {props.titulo}</h2>
            <div>
                {this.states.seasonList}
            </div>
            
        </div>
    )
}

export default Serie; */